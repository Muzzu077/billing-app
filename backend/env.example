# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/billing-app

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development 

# Optional: JWT secret (set in production)
JWT_SECRET=dev_secret

# Optional: Admin reset defaults (used by scripts/resetAdmin.js if CLI args not provided)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=changeme